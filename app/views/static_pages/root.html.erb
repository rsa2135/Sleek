<script>
  <% if logged_in? %>
    window.currentUSer = <%= render(
      'api/users/user.json.jbuilder',
      user: current_user
    ).html_safe %>
  <% end %>
</script>

<main id="root">This is it</main>
